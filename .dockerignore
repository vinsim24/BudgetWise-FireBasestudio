# Git
.git
.gitignore

# Node
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Next.js build output and cache
.next

# Docker specific files
Dockerfile
.dockerignore

# VSCode
.vscode

# OS generated files
.DS_Store
Thumbs.db

# Firebase App Hosting config (not needed in Docker image)
apphosting.yaml

# ShadCN components config (not needed in runtime image)
components.json

# README (not needed in runtime image)
README.md

# Local environment files - these should be passed to the container at runtime, not built into the image
.env
.env*.local
.env.development
.env.production
